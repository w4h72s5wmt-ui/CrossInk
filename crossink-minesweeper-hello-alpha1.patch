diff --git a/src/activities/home/HomeActivity.cpp b/src/activities/home/HomeActivity.cpp
--- a/src/activities/home/HomeActivity.cpp
+++ b/src/activities/home/HomeActivity.cpp
@@ -24,6 +24,7 @@
 #include "../reader/BookReadingStats.h"
 #include "../reader/BookStatsActivity.h"
 #include "../reader/EpubReaderUtils.h"
+#include "AlertActivity.h"
 #include "BookmarkStore.h"
 #include "ClippingStore.h"
 #include "CrossPointSettings.h"
@@ -61,6 +62,7 @@ enum class HomeMenuAction {
   ReadingStats,
   Bookmarks,
   Notes,
+  Minesweeper,
   FileTransfer,
   Settings,
 };
@@ -73,7 +75,7 @@ struct HomeMenuEntry {
 };
 
 struct HomeMenuEntries {
-  static constexpr int kCapacity = 9;
+  static constexpr int kCapacity = 10;
   std::array<HomeMenuEntry, kCapacity> entries{};
   int count = 0;
 
@@ -279,6 +281,7 @@ void appendHomeMenuItems(HomeMenuEntries& items, bool hasOpdsServers, bool hasRe
   }
 
   items.push({tr(STR_NOTES), BookmarkIcon, HomeMenuAction::Notes});
+  items.push({"Demineur", Chart, HomeMenuAction::Minesweeper});
   items.push({tr(STR_FILE_TRANSFER), Transfer, HomeMenuAction::FileTransfer});
   items.push({tr(STR_SETTINGS_TITLE), Settings, HomeMenuAction::Settings});
 }
@@ -304,6 +307,7 @@ HomeMenuEntries buildMinimalMenuItems(bool hasOpdsServers, bool hasReadingStats,
   }
 
   items.push({tr(STR_NOTES), BookmarkIcon, HomeMenuAction::Notes});
+  items.push({"Demineur", Chart, HomeMenuAction::Minesweeper});
   items.push({tr(STR_FILE_TRANSFER), Transfer, HomeMenuAction::FileTransfer});
   return items;
 }
@@ -600,7 +604,7 @@ static_assert(HomeActivity::kMaxCachedBooks >= LyraCarouselMetrics::values.homeR
 
 int HomeActivity::getMenuItemCount() const {
   const auto& metrics = UITheme::getInstance().getMetrics();
-  int count = 5;  // File Browser, Recents, Notes, File transfer, Settings
+  int count = 6;  // File Browser, Recents, Notes, Demineur, File transfer, Settings
   if (!metrics.homeContinueReadingInMenu && !recentBooks.empty()) {
     count += getVisibleRecentBookCount();
   } else if (metrics.homeContinueReadingInMenu && !recentBooks.empty()) {
@@ -1549,6 +1553,9 @@ void HomeActivity::loop() {
           case HomeMenuAction::Notes:
             onNotesOpen();
             break;
+          case HomeMenuAction::Minesweeper:
+            onMinesweeperOpen();
+            break;
           case HomeMenuAction::FileTransfer:
             onFileTransferOpen();
             break;
@@ -1808,6 +1815,9 @@ void HomeActivity::loop() {
       case HomeMenuAction::Notes:
         onNotesOpen();
         break;
+      case HomeMenuAction::Minesweeper:
+        onMinesweeperOpen();
+        break;
       case HomeMenuAction::FileTransfer:
         onFileTransferOpen();
         break;
@@ -2341,6 +2351,13 @@ void HomeActivity::onNotesOpen() {
   startActivityForResult(std::make_unique<NotesActivity>(renderer, mappedInput), [](const ActivityResult&) {});
 }
 
+void HomeActivity::onMinesweeperOpen() {
+  std::strcpy(APP_STATE.pendingAlertTitle, "Demineur");
+  std::strcpy(APP_STATE.pendingAlertBody, "HELLO");
+  APP_STATE.pendingAlertGoHomeOnBack.store(false, std::memory_order_relaxed);
+  startActivityForResult(std::make_unique<AlertActivity>(renderer, mappedInput), [](const ActivityResult&) {});
+}
+
 void HomeActivity::onOpdsBrowserOpen() { activityManager.goToBrowser(); }
 
 void HomeActivity::onReadingStatsOpen() {
diff --git a/src/activities/home/HomeActivity.h b/src/activities/home/HomeActivity.h
--- a/src/activities/home/HomeActivity.h
+++ b/src/activities/home/HomeActivity.h
@@ -91,6 +91,7 @@ class HomeActivity final : public Activity {
   void onReadingStatsOpen();
   void onSavedItemsOpen();
   void onNotesOpen();
+  void onMinesweeperOpen();
 
   int getMenuItemCount() const;
   bool storeCoverBuffer();    // Store frame buffer for cover image
