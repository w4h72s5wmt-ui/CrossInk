diff --git a/src/activities/home/NotesActivity.cpp b/src/activities/home/NotesActivity.cpp
--- a/src/activities/home/NotesActivity.cpp
+++ b/src/activities/home/NotesActivity.cpp
@@ -130,5 +130,6 @@ void NotesActivity::editNote(const std::string& path, const std::string& title) {
           LOG_ERR("NOTES", "Failed to save note: %s", path.c_str());
         }
         reloadNotes();
+        requestUpdate();
       });
 }
@@ -151,6 +152,7 @@ void NotesActivity::createNote() {
           return;
         }
         reloadNotes();
+        requestUpdate();
         editNote(path, keyboard->text);
       });
 }
diff --git a/src/activities/util/KeyboardEntryActivity.cpp b/src/activities/util/KeyboardEntryActivity.cpp
--- a/src/activities/util/KeyboardEntryActivity.cpp
+++ b/src/activities/util/KeyboardEntryActivity.cpp
@@ -933,6 +933,6 @@ void KeyboardEntryActivity::render(RenderLock&&) {
   fui::KeyboardProps props;
   const fui::KeyboardLayout& layout = currentLayout();
   props.layout = &layout;
   props.keyAction = ACTION_KEY;  // one action id; loop() dispatches on key value
-  props.okLabel = tr(STR_OK_BUTTON);
+  props.okLabel = inputType == InputType::Multiline ? "↵" : tr(STR_OK_BUTTON);
   props.shiftLabel = tr(STR_KEY_SHIFT);
